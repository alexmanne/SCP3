

def read_files():
    """ This function will read in the combined data from multiple runs. It will accept
    a list of potentially multiple tuples of the protein data (pg_matrix) and the 
    peptide data (pr_matrix). 

    Parameters:
        file_list (list): A list of tuples of files (protein data, peptide data)
            ex: [("report1.pg_matrix.tsv", "report1.pr_matrix.tsv"), ("report2.pg_matrix.tsv", "report2.pr_matrix.tsv")]
    
    """
    pass

